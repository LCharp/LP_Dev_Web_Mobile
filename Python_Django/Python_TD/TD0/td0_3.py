def Recherche(liste, entite):
vf=false
for i in liste
    if entite==liste[i]
        vf=true
return vf


def indice_for1(l,e):
    for i, x in enumerate(l):
        if l[i]==0:
            return 1
        return None

def indice(l,e):
    return l.index(e)

